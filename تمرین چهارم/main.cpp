#include <iostream>

using namespace std;//سه عدد از ورودی دریافت کنید و بزرگترین عدد راتشخیص دهید
{

int main()
    int a;
    int b;
    int c;
    cin>>a>>b>>c
    cout << "enter three numbers:" << endl;
    if(a>b&&b>c){
    cout<<"a is the greatest.";

    elseif (b>c&&c>a)
    cout<<"b is the greatest.";
    else
    cout<<"c is the greatest.";
        }
    return 0;
}
